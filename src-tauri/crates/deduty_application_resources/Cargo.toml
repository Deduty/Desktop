[package]
name = "deduty_application_resources"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
deduty_package_traits = { path = "../deduty_package_traits"}
